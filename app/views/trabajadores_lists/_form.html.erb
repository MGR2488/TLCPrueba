<%= form_with(model: trabajadores_list, local: true) do |form| %>
  <% if trabajadores_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trabajadores_list.errors.count, "error") %> prohibited this trabajadores_list from being saved:</h2>

      <ul>
      <% trabajadores_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombres %>
    <%= form.text_field :Nombres %>
  </div>

  <div class="field">
    <%= form.label :Apellidos %>
    <%= form.text_area :Apellidos %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
