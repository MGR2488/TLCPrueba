<%= form_with(model: tlcp_rueba_list, local: true) do |form| %>
  <% if tlcp_rueba_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tlcp_rueba_list.errors.count, "error") %> prohibited this tlcp_rueba_list from being saved:</h2>

      <ul>
      <% tlcp_rueba_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
